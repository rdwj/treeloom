function transform(input: string): string {
    let x: string = input;
    let y = x;
    return y;
}

function multiAssign(a: number, b: number): number {
    let result = a;
    result = b;
    return result;
}

const value = transform("hello");
